program MyTest(input, output);
var
    done, a, b, i,j:integer;
    myArray:array [0..10] of integer;
    procedure test();
    begin
        write(0);
    end
begin

    a:=(5+a)*j;

    myArray[5] := 12;
    
    if i = 0 then
        write(0)
    else write(99);
    
    while done = 0 do begin
        read(i);
    end
end